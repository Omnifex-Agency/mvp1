# OpenAI API Key (Required for Summary/Quiz)
OPENAI_API_KEY=sk-...

# AWS Region (Optional, defaults to your AWS config usually)
AWS_REGION=us-east-1

# Sender Email for SES (Must be verified in SES)
SENDER_EMAIL=your-verified-email@example.com
